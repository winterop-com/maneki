{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Maneki desktop default capability — picker page needs the store plugin to remember server URLs, the window-drag command for our custom in-app title bar, native fullscreen for the player's `f` key, and core defaults for everything else.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "store:default"
  ]
}
